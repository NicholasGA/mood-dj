# Mood DJ — 夜间功能开发计划（给 /loop 跑）

> 一夜自跑的**功能**待办。挑的都是**逻辑可机器验证、不靠"听感/看感"、不烧 Gemini 配额**的活；
> UI 长相 / 音频效果 / 口味准不准，留给早上人工过一眼。

## 工作规则（loop 必须遵守）
1. **一个功能 = 一个(或几个)小提交**。每次先 `npm test` + `npm run build` 绿，再 `git push`，再看 GitHub Actions 绿，才做下一个。
2. **纯逻辑必须配 vitest 测试**（队列增删、历史去重、定时器倒计、快捷键映射、歌词跳转命中行…）。
3. **UI/音频/手感无法自检**：保持改动**additive、模块化、易回退**（各功能独立提交）；在「进度」里写一行**"早上请眼检 X"**。
4. **绝不烧 Gemini 配额**：不跑任何 AI 自测循环；涉及 AI 的功能只写逻辑、不联调（今晚配额已 429，明天太平洋午夜才回）。
5. **QQ 账号写操作**只允许「导出歌单」这一项，且用显眼名字（如 `Mood DJ 测试-时间戳`）便于早上识别；**绝不删除**任何已有歌单/收藏。
6. 碰到**真需要产品/UX 拍板**的（叫什么、放哪、默认开还是关）→ **停下，在「进度」留问题**，别猜，跳去做下一个能独立完成的。
7. 做完把 `[ ]` 勾成 `[x]`，并在底部「进度」追一行（做了啥 / 早上要看啥 / 有没有卡）。

## A 队列（最稳，优先）

- [x] **歌词翻译显示开关**：已存在！`Lyrics.jsx` 有 `译/原` 切换(`showTrans`)，开则显示 `l.trans`。无需重做。
- [x] **点击歌词跳转**：已存在！`Lyrics.jsx` 的 `seekTo` 点行即跳。无需重做。
- [x] **播放队列面板**：点「队列 N」徽章打开；播某首/🔥置顶/👎移除/清空。radio.js removeAt/moveToFront 纯函数+测试 + QueuePanel 组件。(c7cfc29)
- [x] **睡眠定时器**：标题栏 🌙 循环 关/15/30/60，末15s 渐降音量到点暂停。纯函数(remainingLabel/sleepVolume/nextDuration)+测试。(c99e3fb)

## B 队列（好做，轻验证）

- [x] **全局键盘快捷键**：空格=播放/暂停、→=下一首、←=快退5s、↑↓=音量、L=喜欢、M=静音。`keyToAction` 纯映射 + 测试；App 挂 keydown。(9d97f12)
- [x] **播放历史**：队列面板加「最近」标签，记 memory.history(新→旧/去重/封顶100)，点歌重听 + 相对时间。pushHistory 纯函数+测试。(db6004f) ｜ 再 ❤️ 暂未加，留作小增量。
- [x] **进度条拖动 seek**：已存在！NowPlaying 的 `seekToRatio`/`onBarDown`/`ratioFromEvent` 已是完整拖动+knob。无需重做。

## C 队列（锦上添花 / 偏手感，默认保守）

- [ ] **设置扩充**：⚙️ 里加「默认心情」「每日 AI 预算(DAILY_LIMIT)可调」「强调色」。配置走已有 `store-config`。纯表单，逻辑可测（配置读写）。
- [ ] **切歌渐入渐出**（gapless-ish）：用 Web Audio gain 在切歌时做 200ms 淡出/淡入。ramp 逻辑可写可测，但**听感必须你早上验**，默认做成设置项、**默认关**。

## 拓展（需 QQ API 摸索，不碰 AI，会在你账号建测试歌单）

- [x] **把当前电台/最近导出成 QQ 歌单**：摸到了 `music.musicasset.PlaylistBaseWrite/AddPlaylist`(签名版)建歌单返回 dirId，再 AddSonglist 灌歌。队列面板两个标签都有「导出歌单」按钮。(dec9df8) ｜ 探接口时建了个 `Mood DJ 测试-…` 测试歌单，可删。

## ⚠️ 不要碰（需先问你）
- 改主播放流程的默认行为、删现有功能、动 electron 主进程的关键路径（取流/协议/窗口）。
- 任何要靠"听一耳朵 / 看一眼才知道对不对"的**默认开启**项——做成开关、默认关，等你验。

## 进度
- 2026-06-09 夜：计划建立。基础设施已就绪（vitest 43 测试 + CI + 签名收藏 + 画像本地兜底）。
- 01:45 ✅ 全局键盘快捷键 (9d97f12)。+4 测试(共47)，build/CI 绿。早上眼检：输入时空格不触发暂停、音量手感。
- 夜间循环已启动，目标 07:00 停。
- 02:10 ℹ️ 发现 A 队列「歌词翻译开关」「点歌词跳转」**原本已存在**于 Lyrics.jsx，标记完成、无需重做。
- 02:22 ✅ 睡眠定时器 (c99e3fb)。+4 测试(共51)。早上眼检：🌙 按钮排版/倒计时/淡出。
- 02:32 ✅ 播放队列面板 (c7cfc29)。+4 测试(共55)。早上眼检：徽章可点/面板交互/排版。
- **【早间总结 12:15】自动跑通宵没实现**：02:32 后到 12:15 零提交——自动唤醒(ScheduleWakeup/loop)在用户离开后未触发（电脑睡眠 / 本环境不支持无人值守后台唤醒）。结论：**这环境里"无人值守跑整夜"做不到**，功能开发得在用户在场时连续做。
- **本夜实际完成（均绿、已提交、共 55 测试通过）**：键盘快捷键(9d97f12)、睡眠定时器(c99e3fb)、播放队列面板(c7cfc29)；并确认歌词翻译开关/点歌词跳转原已存在。
- **请早上眼检**：① 快捷键在输入框打字时空格不应暂停、音量手感；② 标题栏 🌙 排版/倒计时/到点淡出；③「队列 N」徽章可点、队列面板播放/置顶/移除/清空。
- **剩余未做**（用户在场时可继续）：播放历史、进度条拖动 seek、设置扩充、切歌渐入渐出、导出 QQ 歌单。
- 【12:15 起 用户在场连续做】✅播放历史(db6004f) ✅(发现进度条拖动已存在) ✅导出QQ歌单(dec9df8)。剩：设置扩充、切歌渐入渐出。
- ⚠️ 导出歌单建了测试歌单「Mood DJ 测试-…」，用户可在 QQ 删。

# Mood DJ — 夜间功能开发计划（给 /loop 跑）

> 一夜自跑的**功能**待办。挑的都是**逻辑可机器验证、不靠"听感/看感"、不烧 Gemini 配额**的活；
> UI 长相 / 音频效果 / 口味准不准，留给早上人工过一眼。

## 工作规则（loop 必须遵守）
1. **一个功能 = 一个(或几个)小提交**。每次先 `npm test` + `npm run build` 绿，再 `git push`，再看 GitHub Actions 绿，才做下一个。
2. **纯逻辑必须配 vitest 测试**（队列增删、历史去重、定时器倒计、快捷键映射、歌词跳转命中行…）。
3. **UI/音频/手感无法自检**：保持改动**additive、模块化、易回退**（各功能独立提交）；在「进度」里写一行**"早上请眼检 X"**。
4. **绝不烧 Gemini 配额**：不跑任何 AI 自测循环；涉及 AI 的功能只写逻辑、不联调（今晚配额已 429，明天太平洋午夜才回）。
5. **QQ 账号写操作**只允许「导出歌单」这一项，且用显眼名字（如 `Mood DJ 测试-时间戳`）便于早上识别；**绝不删除**任何已有歌单/收藏。
6. 碰到**真需要产品/UX 拍板**的（叫什么、放哪、默认开还是关）→ **停下，在「进度」留问题**，别猜，跳去做下一个能独立完成的。
7. 做完把 `[ ]` 勾成 `[x]`，并在底部「进度」追一行（做了啥 / 早上要看啥 / 有没有卡）。

## A 队列（最稳，优先）

- [x] **歌词翻译显示开关**：已存在！`Lyrics.jsx` 有 `译/原` 切换(`showTrans`)，开则显示 `l.trans`。无需重做。
- [x] **点击歌词跳转**：已存在！`Lyrics.jsx` 的 `seekTo` 点行即跳。无需重做。
- [ ] **播放队列面板**：看接下来要播的，支持移除某首 / 置顶某首 / 清空。抽 `removeAt / moveToFront / dedupe` 纯函数到 `radio.js` + 测试；UI 复用现有面板样式。早上眼检：交互顺手否。
- [ ] **睡眠定时器**：选 15/30/60 分钟，到点把音量渐降到 0 再暂停。抽倒计时/渐降步进为纯函数 + 测试；UI 一个小下拉/按钮。

## B 队列（好做，轻验证）

- [x] **全局键盘快捷键**：空格=播放/暂停、→=下一首、←=快退5s、↑↓=音量、L=喜欢、M=静音。`keyToAction` 纯映射 + 测试；App 挂 keydown。(9d97f12)
- [ ] **播放历史**：把 `recentMids` 升级为带时间戳的历史 `[{mid,name,artists,at}]`，持久化；一个"最近播放"小视图，可点回听 / 再 ❤️。抽"压栈去重封顶"纯函数 + 测试（已有 `pushRecent` 可借鉴）。
- [ ] **进度条拖动 seek**：NowPlaying 进度条支持点击/拖动跳转（`currentTime = ratio*duration`）。抽 ratio↔time 纯函数 + 测试。早上眼检：拖动手感。

## C 队列（锦上添花 / 偏手感，默认保守）

- [ ] **设置扩充**：⚙️ 里加「默认心情」「每日 AI 预算(DAILY_LIMIT)可调」「强调色」。配置走已有 `store-config`。纯表单，逻辑可测（配置读写）。
- [ ] **切歌渐入渐出**（gapless-ish）：用 Web Audio gain 在切歌时做 200ms 淡出/淡入。ramp 逻辑可写可测，但**听感必须你早上验**，默认做成设置项、**默认关**。

## 拓展（需 QQ API 摸索，不碰 AI，会在你账号建测试歌单）

- [ ] **把当前电台/历史导出成一个 QQ 歌单**：签名写已通（见 `qqSign.cjs`）。需先摸 `CreatePlaylist`(或 `PlaylistCreate`) 接口（像之前摸收藏那样、签名版 musics.fcg），建好后 `AddSonglist` 把歌灌进去。**建的歌单用名字 `Mood DJ 测试-<时间>`**，早上你看一眼对不对、要不要正式做成功能。

## ⚠️ 不要碰（需先问你）
- 改主播放流程的默认行为、删现有功能、动 electron 主进程的关键路径（取流/协议/窗口）。
- 任何要靠"听一耳朵 / 看一眼才知道对不对"的**默认开启**项——做成开关、默认关，等你验。

## 进度
- 2026-06-09 夜：计划建立。基础设施已就绪（vitest 43 测试 + CI + 签名收藏 + 画像本地兜底）。
- 01:45 ✅ 全局键盘快捷键 (9d97f12)。+4 测试(共47)，build/CI 绿。早上眼检：输入时空格不触发暂停、音量手感。
- 夜间循环已启动，目标 07:00 停。下一个：A 队列「歌词翻译显示开关」。

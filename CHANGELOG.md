# 更新日志

本项目版本号遵循 `主.次.修` 简单递增。

## [1.0.1]

会记得你的 AI DJ + 每首歌讲故事。

### 新增
- **会记得你的 DJ**：你喜欢的歌成为 DJ 的记忆——心情电台按你的常听口味偏向选歌，
  串场时点名你爱的歌手（QQ 静默算法做不到的）。
- **每首歌一句「故事」**：拉取歌词（优先副歌）喂给 Gemini，点出这首歌的情绪/主题/创作背景。
  歌真正听了约 4s 才生成（跳过不耗配额）、按歌曲永久缓存复用、AI 不可用时有本地兜底。
- 「我喜欢的」面板差异化：**音乐画像**（AI 读你的口味）/ **自动 vibe 分组**，结果按口味缓存不重复烧 API。

### 改动
- 语音播报节流（不再每首都念）+ 播报时自动把音乐压低（像电台），不再盖过音乐。
- 「我喜欢的」面板 UI 统一为矢量图标。
- 移除与 QQ音乐「私人FM/心动模式」重复的「喜欢电台」，把口味数据转去喂 DJ。
- key 限流冷却 30min → 90s（RPM 限流约 60s 即恢复，避免一次限流半小时不可用）。

### 修复
- 应用内存储的坏 API key 会覆盖 `.env` 的好 key，导致 AI 全部失效（画像/故事/选歌）。

### 工程
- 引入 vitest 单元测试（纯逻辑：歌词/副歌/QQ 映射/点歌解析/key 配置）。
- 加 GitHub Actions CI：push/PR 跑 build + test。
- 清理死代码（已弃用的 `generateAnnouncement`）。

## [1.0.0]

首个发布版本。

- 自然语言心情输入 → Gemini 分析 → QQ音乐搜索/播放的 AI 电台。
- 无限续播 + 去重、专辑色主题、FFT 音频可视化、同步歌词（含副歌跳转/翻译）。
- 系统媒体键、托盘 + 迷你播放器、情境感知心情建议。
- 播放中实时调味、跨会话口味记忆、和 DJ 对话点歌。
- 应用内 API key 配置、新手引导、electron-builder 打包、electron-updater 自动更新。

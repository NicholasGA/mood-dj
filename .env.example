# 复制此文件为 .env，填入你的 Key

# Google Gemini API Key（用于心情分析 + AI 选歌 + DJ 串场词）
# 去 https://aistudio.google.com/apikey 获取
# 多 key 轮换：用逗号分隔填多个（每个 key 各有独立免费配额，限流自动换下一个）。
#   不同 Google 账号各建一个 key，配额叠加，基本不会再撞限流。
VITE_GEMINI_API_KEY=key1,key2,key3

# 可选：覆盖默认模型。默认 gemini-2.5-flash-lite（免费日配额高）。
# 注意 gemini-2.5-flash 免费层仅 20 次/天，不够用，别改回去。
# VITE_GEMINI_MODEL=gemini-2.5-flash-lite

# 可选备用 Provider：所有 Gemini key 都限流后兜底用 OpenRouter（OpenAI 兼容，有免费模型）。
# 去 https://openrouter.ai/keys 免费注册拿 key（免费模型够用）。
# VITE_OPENROUTER_API_KEY=sk-or-...
# VITE_OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free

# 可选：内置共享 AI 代理地址（零配置开箱即用）。填了之后，没配自己 key 的用户
# 首启即可用 AI（key 存在服务端 Worker、按 IP 限流）。部署见 proxy/README.md。
# VITE_LLM_PROXY_URL=https://mooddj-llm-proxy.xxx.workers.dev

# QQ 音乐无需 API Key：首次启动时在弹窗内登录 QQ 账号，
# cookie 会被本地保存（userData/mooddj-qq.json）。
